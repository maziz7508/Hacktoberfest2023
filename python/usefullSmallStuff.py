# Things that are too small to be their own files, but come in handy

# Integer.max min
nax = float('-inf')

# Lambda Operations
operation = lambda x, y: x+y
operation(1,2)

# Modulo 10^9+7
M = 1000000007
res %= M

# Filter Function -> Returns ls
filter(fucntion, sequence)
filter(str.isalnum, s)
filter(lambda x: x%2==0, ls)